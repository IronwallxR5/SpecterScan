# ==================================================
# SpecterScan Frontend — Environment Variables
# ==================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to Git — it's in .gitignore.
# Vite only exposes variables prefixed with VITE_ to browser code.

# --- Backend API URL (CORS policy: must match what backend allows) ---
# This is the base URL of the FastAPI backend.
# Make sure this origin is listed in ALLOWED_ORIGINS in the backend .env.
VITE_API_URL=http://localhost:8000
